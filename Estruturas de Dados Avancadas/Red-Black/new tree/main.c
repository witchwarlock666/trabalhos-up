#include "tree.c"

int main() {
    Tree *tree = initTree();

    insertNo(tree, 5);
    insertNo(tree, 11);
    insertNo(tree, 3);
    insertNo(tree, 22);
    insertNo(tree, 8);
    insertNo(tree, 6);
    insertNo(tree, 7);
    insertNo(tree, 9);
}